{% extends 'base.html.twig' %}

{% block body %}
    <div class="banniere">
        <img src="{{ asset('images/banniere.png') }}" alt="homme_noir" />
    </div>
    <div class="container">
        <div class="row">
            <div class="col-xs-6">
                <p>{{ event.titre }}</p>
                <p>{{ event.description }}</p>
                <p>{{ event.descriptionComplementaire }}</p>
            </div>
            <div class="col-xs-6">
                <p>{{ event.lieu }}</p>
                <p>{{ event.adresse }}</p>
                <p>{{ event.ville }}</p>
                <p>{{ event.cp }}</p>
            </div>
        </div>
    </div>

{{ event.nbParticipants }}
    <form method="get" action="{{path('monresultatok',{'id': event.id})}}">
    <input name="participer" value="J'y participe !" type="submit"></td>
    </form>

        button bootstrap href : {{path('monresultatok',{'id': event.id})}}

{% endblock %}
